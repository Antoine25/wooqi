language: python
python:
  - "2.7"
  - "3.4"

# command to install dependencies
install:
  - pip install -e .

script:
  - wooqi --seq-config testing/uut_feature/actions/uut_different_actions.ini --sn wooqi_tests -s
  - ./run_wooqi_tests.sh
